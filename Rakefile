require 'rake'

desc 'Serve site locally'
task :serve do
    sh 'bundle exec jekyll serve --source ./src/'
end

desc 'Build site'
task :build do
    sh 'bundle exec jekyll build --source ./src/'  
end